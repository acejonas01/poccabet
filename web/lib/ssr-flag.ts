// Imported first by SiteApp: tells the shared code this page came from the server, so saved
// state (login, slip, tabs…) is read after the first render instead of during it.
if (typeof window !== "undefined") window.__POCCA_SSR__ = true;
export {};
